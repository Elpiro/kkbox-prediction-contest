1. Run first : 
merge_initial_dataframes.R

Then any Rmarkdown using the following command, for getting Rmarkdown files in HTML :

setwd("directory of Rmd files")
rmarkdown::render("file.Rmd")

2. Open any HTML documents in /RMarkdown to get results without running
